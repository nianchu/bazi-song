# 案例数据规范

每个案例包含以下章节：

1. `状态与隐私`：匿名编号、同意范围、时间不确定性。
2. `排盘`：四柱、性别、城市级出生地、候选时柱。
3. `留出信息`：首次分析前未知的经历。
4. `预先判断`：原始断语、置信度、推理链。
5. `真实反馈`：用户提供的事实，不润色成命理术语。
6. `评分与错误`：命中、过断、错断、未验证。
7. `可复验假设`：只写可被新案例检验的规则。
8. `教学价值`：该案例适合讲授的知识点。

禁止记录真实姓名、精确住址、单位、联系方式、身份证号或未获允许的第三方隐私。
